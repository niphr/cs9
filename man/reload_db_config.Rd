% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_onLoad.R
\name{reload_db_config}
\alias{reload_db_config}
\title{Reload the database configuration from the environment}
\usage{
reload_db_config()
}
\value{
\code{invisible(NULL)}, called for its effect on \code{config}.
}
\description{
Re-reads every \verb{CS9_DBCONFIG_*} environment variable and rebuilds the four
configuration tables.
}
\details{
\code{cs9} reads the environment once, in its own \code{.onLoad()}. A package that sets
its own \verb{CS9_DBCONFIG_*} values inside \code{.onLoad()} therefore sets them too
late: \code{cs9} is a dependency, so it loads first and has already read the
environment by the time the dependent package runs. Call this function
immediately after the \code{Sys.setenv()} block and \code{cs9} picks the new values up.

It re-runs \code{set_env_vars()}, which rebuilds \code{config$dbconfigs}, and then
\code{setup_database_tables()}, which rebuilds \code{config$tables}. Neither opens a
database connection: \code{csdb::DBTable_v9} creates its table lazily, on first
use.
}
\section{State safety}{

The reload is safe against both a failed reload and a repeated one.

It disconnects every table it is about to discard, so a second reload does
not leak the connections held by the R6 objects it replaces.

It then empties \code{config$tables} before it touches \code{config$dbconfigs}. A
failure anywhere after that point therefore leaves \code{config$tables} empty
rather than holding tables built from the previous configuration. An empty
table list is an honest "not configured"; a stale one describes a database
that \code{config$dbconfigs} no longer points at.
}

\examples{
\dontrun{
# In a dependent package's .onLoad(), before its own setup runs
Sys.setenv(CS9_DBCONFIG_ACCESS = "config/anon")
Sys.setenv(CS9_DBCONFIG_DRIVER = "SQLite")
Sys.setenv(CS9_DBCONFIG_DB_CONFIG = "/tmp/config.sqlite")
Sys.setenv(CS9_DBCONFIG_DB_ANON = "/tmp/anon.sqlite")
cs9::reload_db_config()
}
}
\seealso{
\code{\link[=check_environment_setup]{check_environment_setup()}}, which reports whether the environment
is complete.
The installation vignette, \code{vignette("installation", package = "cs9")}.
}
