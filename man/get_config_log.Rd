% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_log.R
\name{get_config_log}
\alias{get_config_log}
\title{Get Configuration Log}
\usage{
get_config_log(ss = NULL, task = NULL, start_date = NULL, end_date = NULL)
}
\arguments{
\item{ss}{Character. Surveillance system identifier to filter by. Defaults to \code{NULL} (no filtering).}

\item{task}{Character. Task name to filter by. Defaults to \code{NULL} (no filtering).}

\item{start_date}{Character. Start date (\code{YYYY-MM-DD}) for filtering log entries. Defaults to \code{NULL}.}

\item{end_date}{Character. End date (\code{YYYY-MM-DD}) for filtering log entries. Defaults to \code{NULL}.}
}
\value{
A \code{data.table} containing the filtered log entries.
}
\description{
Retrieves configuration log entries from the \code{config_log} table with optional filtering
by surveillance system identifier, task name, and date range.
}
\details{
The function retrieves entries from the \code{config_log} table in the current configuration.
The function applies any date filters to the \code{timestamp} field of the log entries.
}
\examples{
\dontrun{
# Get all log entries
get_config_log()

# Get logs for a specific surveillance system
get_config_log(ss = "weather")

# Get logs for a specific task and date range
get_config_log(task = "data_import", start_date = "2024-01-01", end_date = "2024-12-31")
}

}
